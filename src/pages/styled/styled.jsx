import React from "react";
import styled from "styled-components";

const Text = styled.div`
  color: red;
  font-size: 28px;
`;

const StyledComponent = () => {
  return <Text>I am a styled component</Text>;
};

export default StyledComponent;
